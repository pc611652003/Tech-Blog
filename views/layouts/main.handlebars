<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
     <header>
      <nav>
        <a href="/"><button id="homepage-btn" class="btn-no-style">Homepage</button></a>
        <a href="/dashboard"><button id="dashboard-btn" class="btn-no-style">Dashboard</button></a>
        {{#if loggedIn}}
        <button id="logout-btn" class="btn-no-style">Logout</button>
        {{else}}
        <a href="/login"><button id="login-btn" class="btn-no-style">Login</button></a>
        {{/if}}
      </nav>
     </header>
     <h1 class="blog-title">
        <a href="/">The Tech Blog</a>
     </h1>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>